/* base type */
let num:number = 5;
let str:string = "name";
let isOwner:boolean = false;


const notValue:undefined = undefined;
const city:null = null;


const hasGf:unknown = null;
let dogName:any = "m2";
let dogName2:any = 213; //может принять себя как число, строка или булеан, короче все типы. Но избегать использования 

let entity:void; // это проста пустота и обычно используется когда мы используем функцию. пример:
() => console.log("erg") // это функция по сути ничего не возращает потому что нет return и поэтому не возв. void




const firstName:string = "Rauf";
let age:number;
age = 25;

let hasJob:boolean = false;
let profile:undefined = undefined;

let cityInfo:null = null;
let hasGf4: unknown = null;

let dogName4: any = "wee";

